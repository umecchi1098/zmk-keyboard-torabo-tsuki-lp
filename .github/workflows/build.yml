name: Build ZMK firmware
on: [push, pull_request, workflow_dispatch]

jobs:
  build:
    # ローカル検証と同じDYA向けZMKコミットのビルド環境を使用します。
    uses: cormoran/zmk/.github/workflows/build-user-config.yml@e5c9b6915b56801193e359dd9bad4a167ce0d1b8
