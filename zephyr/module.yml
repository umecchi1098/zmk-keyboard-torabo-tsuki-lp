build:
  cmake: .
  # このリポジトリ固有のKconfig設定をZephyrのビルドへ読み込ませます。
  kconfig: Kconfig
  settings:
    board_root: .
    # このリポジトリ固有のDevicetree bindingとvendor prefixを読み込ませます。
    dts_root: .
    snippet_root: .
